<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema">

    <xs:element name="orders">
        <xs:complexType>
            <xs:sequence>
                <xs:element name="order" maxOccurs="unbounded">
                    <xs:complexType>
                        <xs:sequence>
                            <xs:element name="product" maxOccurs="unbounded">
                                <xs:complexType>
                                    <xs:sequence>
                                        <xs:element name="name" type="xs:string"/>
                                        <xs:element name="quantity" type="xs:integer"/>
                                    </xs:sequence>
                                    <!-- id của product, chỉ cần duy nhất trong từng order -->
                                    <xs:attribute name="id" type="xs:string" use="required"/>
                                </xs:complexType>
                            </xs:element>
                        </xs:sequence>
                        <!-- id của order là duy nhất toàn tài liệu -->
                        <xs:attribute name="id" type="xs:ID" use="required"/>
                        <xs:attribute name="customer" type="xs:string" use="required"/>
                        <xs:attribute name="date" type="xs:date" use="required"/>
                    </xs:complexType>

                    <!-- Ràng buộc product/@id duy nhất trong phạm vi order -->
                    <xs:unique name="UniqueProductID">
                        <xs:selector xpath="product"/>
                        <xs:field xpath="@id"/>
                    </xs:unique>
                </xs:element>
            </xs:sequence>
        </xs:complexType>
    </xs:element>

</xs:schema>
